[project]
name = "workshop"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "madvith-d", email = "madroux930@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "google-genai>=2.24.0",
]

[project.scripts]
workshop = "workshop:main"

[build-system]
requires = ["uv_build>=0.12.17,<0.13.0"]
build-backend = "uv_build"
